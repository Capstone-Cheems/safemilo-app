import React from 'react'
import { View, Text } from 'react-native'
import LoaderForView from '@/components/LoaderForView'

const Index = (): React.JSX.Element => {
    return (
        <View>
            <LoaderForView />
        </View>
    )
}

export default Index
