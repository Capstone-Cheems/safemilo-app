import { View, Text } from 'react-native'
import React from 'react'

const setting = (): React.JSX.Element => {
    return (
        <View>
            <Text>setting</Text>
        </View>
    )
}

export default setting
